# Data Types

int
float
bool
str
list
tuple
set
dict

None

# type()
# incompatible values like int('s') leads to error in python, but not in javascript

# these data type classes are on the same level
# in javascript, object is parent class of Array

10 // 3  # 3 --> integer division, not available in javascript

# advanced math functions globally available in python, inS Math object in javascript

# len(iterable) for length of iterables

# format strings f'{} {}'

# accessing out of bounds index of an iterable raises an error in python

# creating slices of iterable - iterable[start:end:step]
# in slicing, leaving start blank will have it default to 0
# in slicing, leaving end blank will have it default to len(iterable)

# creating copy of iterable -  iterable[:]
# creating a reversed copy of iterable - iterable[::-1]

# range(x) will be 0 to x - 1
# range(1, 5) will be 1, 2, 3, 4
# range(5, -1, -1) will be 5, 4, 3, 2, 1, 0
# range(len(l)) to iterate over indices of iterable
# range(len(l) - 1, -1, -1) to iterate over indices of iterable backwards

# falsy values - False, None, 0, 0.0, [], {}, (), '', set(), range(0)
